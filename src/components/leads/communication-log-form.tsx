"use client";

/** Deprecated — contact attempts / communication log removed from simplified CRM. */
export function CommunicationLogForm(_props: {
  leadId: string;
  canLog: boolean;
}) {
  return null;
}
